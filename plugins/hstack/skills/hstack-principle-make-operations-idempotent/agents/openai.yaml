interface:
  display_name: hstack-principle-make-operations-idempotent
  short_description: Within an explicitly selected hstack workflow. Apply when design
policy:
  allow_implicit_invocation: false
