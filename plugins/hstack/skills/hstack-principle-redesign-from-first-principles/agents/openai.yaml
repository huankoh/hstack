interface:
  display_name: hstack-principle-redesign-from-first-principles
  short_description: Within an explicitly selected hstack workflow. Apply when integr
policy:
  allow_implicit_invocation: false
