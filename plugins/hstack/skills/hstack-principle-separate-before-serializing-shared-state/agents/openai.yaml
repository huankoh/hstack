interface:
  display_name: hstack-principle-separate-before-serializing-shared-state
  short_description: Within an explicitly selected hstack workflow. Apply when concur
policy:
  allow_implicit_invocation: false
